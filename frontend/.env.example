VITE_API_URL=https://xxxx.execute-api.us-east-1.amazonaws.com/Prod
# This key is embedded in the JS bundle (publicly readable). It controls rate limits only — NOT a secret.
# Backend requirement: API Gateway Usage Plan with throttling + CORS restricted to the Amplify domain.
VITE_API_KEY=your-api-key-here
